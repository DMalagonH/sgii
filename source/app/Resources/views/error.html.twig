<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
		<style>
			*{
				margin:0;
				padding:0;
			}
			body{
                background:url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAoAAAAKCAYAAACNMs+9AAAAUElEQVQYV2NkYGAwBuKzQAwDID4IoIgxIikAMZE1oRiArBDdZBSNMIXoJiFbDZYDKcSmCOYimDuNSVKIzRNYrUYOFuQgweoZbIoxgoeoAAcAEckW11HVTfcAAAAASUVORK5CYII=) repeat;
                background-color:#212121;
				color:white;
				font-size: 18px;
				padding-bottom:20px;
			}
			.error-code{    
				font-size: 200px;
				color: white;
				color: rgba(255, 255, 255, 0.98);
				width: 100%;
				text-align: right;
				margin-top: 5%;
				text-shadow: 5px 5px hsl(0, 0%, 25%);
				float: left;
                text-align: center;
			}
			.not-found{
				width: 100%;
				text-align: center;
				margin-top: 5%;
				font-size: 50px;
				color: white;
				text-shadow: 2px 2px 5px hsl(0, 0%, 61%);
				padding-top: 70px;
			}
			.clear{
				float:none;
				clear:both;
			}
			.content{
				text-align:center;
				line-height: 30px;
			}
			input[type=text]{
				border: hsl(247, 89%, 72%) solid 1px;
				outline: none;
				padding: 5px 3px;
				font-size: 16px;
				border-radius: 8px;
			}
			a{
				text-decoration: none;
				color: #9ECDFF;
				text-shadow: 0px 0px 2px white;
			}
			a:hover{
				color:white;
			}

		</style>
        
{%set mensaje %}
	{% if status_code == 400 %} La solicitud no es correcta
	{% elseif status_code == 401 %}	El cliente no tiene los privilegios necesarios para acceder al sitio
	{% elseif status_code == 403 %} La solicitud está prohibida. Es posible que necesite registrarse en el sitio antes de estar autorizado a acceder a aqui
	{% elseif status_code == 404 %} El recurso solicitado ya no existe o ha sido desplazado, o es posible que la dirección esté mal escrita
	{% elseif status_code == 500 %}	El servidor no pudo tratar la solicitud
	{% elseif status_code == 501 %} El servidor no soporta el tipo de servicio o el protocolo solicitado
	{% elseif status_code == 503 %} El servidor tomá demasiado tiempo para responder y se desconectá
	{% else %} Error no identificado
	{% endif %}
{%endset%}
    
		<title>Error</title>
	</head>
	<body>
		<p class="error-code">{{ status_code }}</p>
		<div class="clear"></div>
		<div class="content">
            {{ mensaje }}
			<br/><a href="{{ path('homepage')}}">Volver al inicio</a><br/>
            {#<form>Search<br/><input autofocus type="text" name="search" /></form>#}
		</div>
	</body>
</html>