{% extends '::base.html.twig' %}

{% block menu %}
    {% set origen = 'acceso_tipo_investigacion' %}
    {% include '::menu_principal.html.twig' %}
{% endblock menu %}

{% block body %}
    <div class="row-fluid sortable">
        <div class="box span12">
            <div class="box-header well" data-original-title>
                <h2><i class="icon-list-alt"></i> Tipos de investigación</h2>
                <div class="box-icon">
                    <a href="{{ path('tipoinvestigacion_new') }}" class="btn btn-round btn-text"><i class="icon-plus"></i> Nuevo</a>
                    <a href="#" class="btn btn-minimize btn-round"><i class="icon-chevron-up"></i></a>
                </div>
            </div>
            <div class="box-content">
                {% if entities %}
                    <table class="table table-striped table-bordered bootstrap-datatable datatable">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th style='text-align:center;'>Estado</th>
                                <th style='text-align:center;'>Opciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for entity in entities %}
                                <tr>
                                    <td>{{ entity.tinNombreTipo }}</td>
                                    <td style='text-align:center;'>
                                        {% if entity.tinEstado == 1 %}
                                            <span class="label label-success">Activo</span>
                                        {% else %}
                                            <span class="label">Inactivo</span>
                                        {% endif %}
                                    </td>
                                    <td style='text-align:center;'>
                                        <a class="btn btn-success" href="{{ path('tipoinvestigacion_show', { 'id': entity.id }) }}">
                                            <i class="icon-zoom-in icon-white"></i>  Ver
                                        </a>
                                        <a class="btn btn-info" href="{{ path('tipoinvestigacion_edit', { 'id': entity.id }) }}">
                                            <i class="icon-edit icon-white"></i> Editar
                                        </a>
                                        {#<a class="btn btn-danger" href="#">
                                            <i class="icon-trash icon-white"></i> Delete
                                        </a>#}
                                    </td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                {% else %}
                    No se ha registrado ningún tipo de investigación
                {% endif %}
            </div>
        </div><!--/span-->
    </div><!--/row-->
{% endblock %}